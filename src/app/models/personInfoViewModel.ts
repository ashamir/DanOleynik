export interface PersonInfoViewModel {
  title: string;
  value: string;
  label: string;
  isActive: boolean;
  backgroundPosition: BackgroundPositionPostion;
}
export interface BackgroundPositionPostion {
  top: string;
  topActive: string;
  left: string;
}
